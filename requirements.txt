# StockBrain Python backend dependencies (verified on Python 3.13).

# --- core API + data ---
fastapi
uvicorn
python-dotenv
yfinance
requests
anthropic
python-frontmatter
beautifulsoup4     # screener.in shareholding scraping

# --- RAG (local, offline embeddings) ---
chromadb
fastembed          # local ONNX embeddings — free, offline, no torch (do NOT add openai/torch)
# NOTE: we use chromadb + fastembed + the Anthropic SDK directly rather than LangChain —
# fewer deps and simpler streaming. No langchain/openai/torch required.

# --- tests ---
pytest             # unit tests for scorer logic
