# StockBrain Python backend dependencies.
#
# NOTE: Pass 1 (the vertical slice: search + stock page) only needs the deps marked
# [slice]. The RAG and signals deps are listed here per the full build brief but are
# installed when those features are built (chromadb/fastembed wheels on Python 3.13
# are verified at that point).

# --- [slice] core API + data ---
fastapi
uvicorn
python-dotenv
yfinance
requests
anthropic
python-frontmatter

# --- RAG (later pass) ---
langchain
langchain-anthropic
langchain-chroma
chromadb
fastembed          # local ONNX embeddings — free, offline, no torch (do NOT add openai/torch)

# --- vault watching + tests (later pass) ---
watchdog
pytest             # unit tests for scorer logic
